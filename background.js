/*
* TEXT SELECTIONS
*/

/*try {
    chrome.tabs.query({ active: true, currentWindow: true }, function (tabs) {
        chrome.tabs.executeScript(
            tabs[0].id,
            { files: ['content.js'] }
    )});
}
catch (e) {
    console.log(e);
} */
